import { OfferRoutePipe } from './offer-route.pipe';

describe('OfferRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferRoutePipe();
    expect(pipe).toBeTruthy();
  });
});

import { OfferByEmployeePipe } from './offer-by-employee.pipe';

describe('OfferByEmployeePipe', () => {
  it('create an instance', () => {
    const pipe = new OfferByEmployeePipe();
    expect(pipe).toBeTruthy();
  });
});

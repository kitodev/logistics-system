import { SelectSeatPipe } from './select-seat.pipe';

describe('SelectSeatPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectSeatPipe();
    expect(pipe).toBeTruthy();
  });
});

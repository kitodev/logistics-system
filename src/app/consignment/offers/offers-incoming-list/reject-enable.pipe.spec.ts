import { RejectEnablePipe } from './reject-enable.pipe';

describe('RejectEnablePipe', () => {
  it('create an instance', () => {
    const pipe = new RejectEnablePipe();
    expect(pipe).toBeTruthy();
  });
});

import { OfferLotWeightSumPipe } from './offer-lot-weight-sum.pipe';

describe('OfferLotWeightSumPipe', () => {
  it('create an instance', () => {
    const pipe = new OfferLotWeightSumPipe();
    expect(pipe).toBeTruthy();
  });
});

import { EmployeeIdentificationExpirationPipe } from './employee-identification-expiration.pipe';

describe('EmployeeIdentificationExpirationPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeIdentificationExpirationPipe();
    expect(pipe).toBeTruthy();
  });
});

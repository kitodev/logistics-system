import { IsReceiverPipe } from './is-receiver.pipe';

describe('IsReceiverPipe', () => {
  it('create an instance', () => {
    const pipe = new IsReceiverPipe();
    expect(pipe).toBeTruthy();
  });
});

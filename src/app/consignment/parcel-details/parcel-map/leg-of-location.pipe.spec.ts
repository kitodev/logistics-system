import { LegOfLocationPipe } from './leg-of-location.pipe';

describe('LegOfLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new LegOfLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
